# Backend Dockerfile
FROM node:18-alpine

WORKDIR /app

# 의존성 설치
COPY backend/package*.json ./
RUN npm ci --only=production

# 소스 코드 복사
COPY backend/ ./

# 환경 변수 설정 (docker-compose에서 주입)
ENV NODE_ENV=production

EXPOSE 3001

# 애플리케이션 실행
CMD ["node", "src/app.js"]


