# Backend Development Dockerfile
FROM node:18-alpine

WORKDIR /app

# 의존성 설치
COPY backend/package*.json ./
RUN npm install

# 소스 코드는 볼륨 마운트로 처리

# 환경 변수 설정
ENV NODE_ENV=development

EXPOSE 3001

# nodemon으로 개발 서버 실행 (핫 리로드)
CMD ["npm", "run", "dev"]


