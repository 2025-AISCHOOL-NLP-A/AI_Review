# 🚀 프로젝트 시작하기

## 한 줄 요약
```bash
docker-compose -f docker-compose.dev.yml up
```
이 명령어 하나로 Frontend + Backend + Model Server 모두 실행!

## 처음 시작하는 경우

### 1. .env 파일 설정
```bash
# .env.example을 복사해서 .env 생성
copy .env.example .env

# .env 파일 열어서 수정
notepad .env
```

**필수 설정:**
- `OPENAI_API_KEY` - OpenAI API 키
- `DB_HOST`, `DB_PORT` 등 - 데이터베이스 정보

### 2. Docker 실행
```bash
# 개발 서버 시작 (로그 보면서)
docker-compose -f docker-compose.dev.yml up

# 또는 백그라운드로 실행
docker-compose -f docker-compose.dev.yml up -d
```

### 3. 브라우저에서 확인
- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:3001
- **Model Server**: http://localhost:8000

## 평소 사용

### 시작
```bash
docker-compose -f docker-compose.dev.yml up -d
```

### 중지
```bash
docker-compose -f docker-compose.dev.yml down
```

### 로그 확인
```bash
# 모든 로그
docker-compose -f docker-compose.dev.yml logs -f

# Frontend만
docker-compose -f docker-compose.dev.yml logs -f frontend

# Backend만
docker-compose -f docker-compose.dev.yml logs -f backend
```

## 코드 수정 시

**아무것도 안 해도 됨!**
- Frontend: 파일 저장하면 브라우저 자동 새로고침
- Backend: 파일 저장하면 서버 자동 재시작
- Model Server: 파일 저장하면 서버 자동 재시작

## Docker 없이 실행하려면

### Frontend
```bash
cd frontend
npm install
npm run dev
```

### Backend
```bash
cd backend
npm install
npm run dev
```

### Model Server
```bash
cd model_server
pip install -r requirements.txt
python -m uvicorn model_server.main:app --reload
```

## 문제 해결

### 포트가 이미 사용 중
```bash
# 포트 확인
netstat -ano | findstr :5173
netstat -ano | findstr :3001
netstat -ano | findstr :8000

# 프로세스 종료
taskkill /PID [프로세스ID] /F
```

### 의존성 에러
```bash
# 컨테이너 재시작
docker-compose -f docker-compose.dev.yml restart
```

### 완전히 새로 시작
```bash
docker-compose -f docker-compose.dev.yml down
docker-compose -f docker-compose.dev.yml up
```

## 장점

✅ **한 번에 실행** - 3개 서버를 한 명령어로
✅ **빠른 개발** - 코드 수정하면 즉시 반영
✅ **환경 통일** - 팀원들과 동일한 환경
✅ **간편한 관리** - Docker가 알아서 관리

## 주의사항

⚠️ 처음 실행 시 의존성 설치로 1-2분 소요
⚠️ .env 파일은 Git에 커밋하지 말 것
⚠️ Docker Desktop이 실행 중이어야 함
